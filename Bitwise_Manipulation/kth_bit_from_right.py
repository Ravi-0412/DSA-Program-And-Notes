
# Logic:  Right shift the number 'k-1' times then the kth bit wil come at rightmost side.
# Now return 0 if number is even else 1 OR simply return this curr number by taking '&' with '1'

# this same logic we can return (N |(1<< K)) in above method also

